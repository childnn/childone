spring.datasource.url=jdbc:mysql://120.78.70.187:3306/xq_db?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false&allowMultiQueries=true
spring.datasource.username=root
spring.datasource.password=lipeng632930871

rabbit：
 用户：xqlive    密码：xqmq123465


CREATE TABLE `act_goods_sku` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `act_id` bigint(20) DEFAULT NULL COMMENT '活动id',
  `sku_id` bigint(20) DEFAULT NULL COMMENT '商品sku_id',
  `sku_code` varchar(20) DEFAULT NULL COMMENT '商品编号(是参与活动生成的编号)',
  `shop_id` bigint(20) DEFAULT NULL COMMENT '商品对应的商家id',
  `category_id` bigint(20) DEFAULT NULL COMMENT '活动类目',
  `goods_pr_id` bigint(20) DEFAULT NULL COMMENT '商品所对应的规则id',
  `apply_status` tinyint(2) DEFAULT NULL COMMENT '审核状态 0 待审批 1 审批通过 2审批不通过',
  `sort_num` int(11) DEFAULT '0' COMMENT '排序顺序',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `is_deleted` int(2) DEFAULT '0' COMMENT '是否删除 0 否 1是',
  `weight` int(4) DEFAULT '1' COMMENT '权重 ,该商品抽奖时的权重',
  `ratio` decimal(4,4) DEFAULT NULL COMMENT '概率，抽奖时的固定概率，有值时不计算权重，例：0.2表示固定20%概率',
  `stock_num` int(11) DEFAULT NULL COMMENT '库存，剩余可抽奖数量，不为0才能参与抽奖',
  `waring_stock` int(11) NOT NULL COMMENT '预警库存',
  `people_num` int(11) DEFAULT NULL COMMENT '参与人数(当前参与商品的指定配置人数)',
  `end_time` datetime DEFAULT NULL COMMENT '商品活动结束时间',
  `rule_desc` varchar(10000) DEFAULT NULL COMMENT '商品活动规则描述',
  `start_time` datetime DEFAULT NULL COMMENT '商品活动开始时间',
  `due_time` datetime DEFAULT NULL COMMENT '商品活动截至时间',
  `create_user_id` bigint(20) DEFAULT NULL COMMENT '创建者id',
  `state` int(2) DEFAULT NULL COMMENT '商品活动状态：1，进行中，2，成功 3，失败',
  `current_num` int(11) DEFAULT '0' COMMENT '当前参与人数(适用于拼团活动,代表该团目前实际参与的人数)',
  `goods_sku_number` varchar(50) DEFAULT NULL COMMENT '编号',
  `vote_num` int(11) DEFAULT '0' COMMENT '投票数目',
  `auto_add_stock` int(2) NOT NULL DEFAULT '0' COMMENT '是否自动补充库存 0 否 1是',
  `expiry_date` datetime DEFAULT NULL COMMENT '有效期时间',
  `discount` decimal(3,2) DEFAULT NULL COMMENT '折扣',
  `weekend_usable` int(2) DEFAULT '0' COMMENT '是否周末通用(0,不通用,1 通用)',
  `time_usable` int(2) DEFAULT '0' COMMENT '是否限制时段(0,不限制,1 限制)',
  PRIMARY KEY (`id`),
  KEY `pk_act_goods_sku_id` (`id`) USING BTREE,
  KEY `idx_sku_id` (`sku_id`) USING BTREE,
  KEY `idx_goods_pr_id` (`goods_pr