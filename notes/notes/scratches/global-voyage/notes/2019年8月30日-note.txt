NO_TICKET sku.
1. 从商品列表进入库存管理页面: 展示 库存属性列表 -- 从 base_property 表获取(type_name = 'NO_TICKET_SKU'/'TICKET_SKU').
     property type 属性值(中文名) 作为前端的展示的汉字属性名.
     property code 属性值作为 command 的属性名, 往后端封装值.
     com.xq.live.globalVoyage.spi.globalVoyage.controller.goods.GoodsSkuInventoryControllerInterface.getProperties
1.p. 选中基础属性后, 点击 "使用以上属性创建库存", 回显已有的 sku 列表.
2. 为每个属性赋值(可多值), 清位时间(天, 时分秒).
3. 创建成功, 展示库存列表: 库存 id(sku id), 库存名称(基础属性), 清位时间, 状态(上线/未上线).
     com.xq.live.globalVoyage.spi.globalVoyage.controller.goods.GoodsSkuInventoryControllerInterface.createNoTicketSkuInventory
4. 修改清位时间.
     com.xq.live.globalVoyage.spi.globalVoyage.controller.goods.GoodsSkuInventoryControllerInterface.editClearTime
5. 库存上线/下线: "上线" 库存后该商品(spu)会自动 "下线"(goods_sku.sku_status = 1, goods_spu.status = 0).
6. 删除库存: is deleted = 1.
     com.xq.live.globalVoyage.spi.globalVoyage.controller.goods.GoodsSkuInventoryControllerInterface.updateStatus
7. 价格编辑:
    单个 sku:
        数据回显: 库存名称, 按日期(月份)回显(成人/儿童/单房差: 售, 余, 购买上/下限)  (活动)
           com.xq.live.globalVoyage.spi.globalVoyage.controller.goods.GoodsSkuInventoryControllerInterface.editPrice
        添加/更新价格:
           com.xq.live.globalVoyage.spi.globalVoyage.controller.goods.GoodsSkuInventoryControllerInterface.createOrUpdateSkuPrice

    批量 sku：

TICKET sku
1. 六种基础属性:
   1.1.p1. 数据回显 -- 六种基础属性.
   1.1.p2. 点击 "使用以上属性创建库存" -- 回显已有的库存列表.
   1.1. 票种类型列表: 数据源来源于后端 base_property 表.
   1.2. 可用天数列表: base_property 表.
2.
