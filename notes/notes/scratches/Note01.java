package miaowan;

/**
 * 2019年2月15日12:17:09
 * CRUD: 曾 查 改 删 （增删改查）
 * create, read, update, delete
 * 逻辑与&&和逻辑或||的短路效果改如何良好运用
 *      &&：左边false，右边不执行
 *      ||：左边true，右边不执行
 *
 * 数据结构
 *      数组：Array
 *          增删慢，查询快
 *          length
 *          size()
 *          add()
 *          set()
 *          get()
 *          remove()
 *
 *      链表：Linked List
 *          增删快，查询慢
 *          对比数组，链表在内存分配、内部结构以及数据插入和删除的基本操作方面均有所不同。
 *      栈：Stack,操作受限的链表
 *          LIFO:Last in,First out
 *      队列：Queue，操作受限的链表
 *          FIFO:First in,First out
 *      树：Tree
 *          层级式的数据结构，由节点和连接节点的边组成
 *      图：Map(Graph)
 *      堆：Heap
 *      散列表(哈希表)：Hash
 */
public class Note01 {
    /**
     * Array:
     *      寻找数组中第二小的元素
     *      找到数组中第一个不重复出现的整数
     *      合并两个有序数组
     *      重新排列数组中的正值和负值
     * Stack:
     *      使用栈计算后缀表达式
     *      对栈的元素进行排序
     *      判断表达式是否括号平衡
     * Queue:
     *      使用队列表示栈
     *      对队列的前k个元素倒序
     *      使用队列生成从1到n的二进制数
     * LinkedList
     *      反转链表
     *      检测链表中的循环
     *      返回链表倒数第N个节点
     *      删除链表中的重复项
     * Map:
     *      实现广度和深度优先搜索
     *      检查图是否为树
     *      计算图的边数
     *      找到两个顶点之间的最短路径
     * Tree:
     *
     */
}
