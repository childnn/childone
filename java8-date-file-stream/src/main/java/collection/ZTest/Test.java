package collection.ZTest;import java.util.Arrays;import java.util.List;/** * Collections工具类：static <T> boolean addAll​(Collection<? super T> c, T... elements) 将所有指定的元素添加到指定的集合。 * <p> * JDK9新特性：接口静态方法//它与接口私有方法有何区别？（private方法不可以被外界访问） * List接口，Set接口，Map接口（仅这三个接口，不包括其实现类，static方法无法被重写） * static <E> List<E> of​(E... elements) 返回包含任意数量元素的【不可变列表】。 */public class Test {    public static void main(String[] args) {        List<Object> list = Arrays.asList(1, 1, 2, 3, 4, 5);        //List<Object> list = List.of(1, 1, 2, 3, 4, 5);        System.out.println("list = " + list);        //        list.add(23); //of()方法返回的集合长度不可变，UnsupportedOperationException,不支持操作异常        //        Set<Object> listS = Set.of(1, 2, 3, 4, 5 , 1);        //        System.out.println("listS = " + listS); //Set集合不可重复。IllegalArgumentException,非法参数异常    }}