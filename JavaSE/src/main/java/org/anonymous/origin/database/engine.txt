MySQL：
    InnoDB：事务型引擎, 增删改, 行锁
    Myisam：非事务型引擎, 读, 不支持外键, 表锁

索引: 读操作多, 主键字段, 夺标连接条件加索引
    经常 order by, group by, distinct 字段加索引

索引失效:
    or, 字段有 null 值, like 以 % 开头,
    索引最左原则: 多列索引, 不使用第一部分则不会使用索引,
    (最常使用的条件应该放在联合索引的左侧)
    字符串必须使用单引号(双引号也可以, 推荐单引号),
    在索引列上使用表达式或函数会使索引失效