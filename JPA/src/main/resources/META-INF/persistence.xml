<?xml version="1.0" encoding="utf-8" ?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence
             http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
             version="2.0">
    <persistence-unit name="jpa-1" transaction-type="RESOURCE_LOCAL">

        <!-- 这些标签有先后顺序 -->
        <description>JPA-HelloWorld</description>
        <!--
            1. 配置 javax.persistence.spi.PersistenceProvider 的实现
            2. 若 JPA 项目只有一个 JPA 实现, 可不配
        -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <class>org.anonymous.model.Customer</class>
        <!--<exclude-unlisted-classes></exclude-unlisted-classes>-->


        <properties>
            <property name="hibernate.ejb.cfgfile" value="/hibernate.cfg.xml"/>

            <!-- db-props -->
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql:///jpa"/>
            <property name="javax.persistence.jdbc.user" value="root"/>
            <property name="javax.persistence.jdbc.password" value="root"/>
            <!-- JPA 实现产品的基本属性, 配置 hibernate 的基本属性 -->
            <property name="hibernate.format.sql" value="true"/>
            <property name="hibernate.show_sql" value="true"/>
            <!-- 是否自动生成 DDL -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
        </properties>


    </persistence-unit>
</persistence>