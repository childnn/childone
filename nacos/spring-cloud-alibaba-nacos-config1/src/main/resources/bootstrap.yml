server:
  port: 9877
spring:
  application:
#    name: another_new_group-config
    name: nacos-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8888,localhost:8858
        # com.alibaba.cloud.nacos.client.NacosPropertySourceLocator#locate
        name: another_new_group-config.yml # data-id 直接指定
#        prefix: # data-id 前缀, com.alibaba.cloud.nacos.client.NacosPropertySourceLocator.locate
        namespace: 2607931d-fa37-4743-81b5-ebdc39781c54
        group: another_new_group
        encode: UTF-8
        file-extension: yml # data-id 间接指定就是 ${spring.application.name}.${file-extension}
        refresh-enabled: true
